/examples/cli/cli
examples/cli/config*.yaml
.env*
.DS_Store
coverage.out
coverage.html
*.pdf
*.var
logs/
improvements.md
docs/superpowers/
scripts/
# Symbol-version hardware test logs
symbol-version-run-*.log

# Self-review agent artifacts
.self-review/

# Local hardware-probe helpers (route-table admin, System Service probes).
# Kept out of the repo because they lean on work-in-progress router code that
# isn't committed yet, so committing them would break the integration build.
zz_*_integration_test.go
